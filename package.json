{
  "name": "guesswho",
  "version": "0.1.0",
  "private": true,
  "description": "A web version of Guess Who for me and my friends to play.",
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 8791 --bind 127.0.0.1",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
